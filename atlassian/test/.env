DC_PREFIX=test

# relative path seems buggy
#COMPOSE_FILE=../docker-compse.yml

# All domain names and ports should match reverse proxy

DC_JIRA_NAME=test-jira.norha.us
DC_JIRA_PORT=18080

DC_BITBUCKET_NAME=test-git.norha.us
DC_BITBUCKET_PORT=17990

# Best not to use standard port to prevent others from accidentally using this db
DC_DB_PORT=15432
DC_DB_PW=uniquePasswd

# must match port if using ssh for git remote set-url
DC_SSH_PORT=17999
